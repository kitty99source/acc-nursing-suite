ACC District Nursing Admin Suite — launchers

Start ACC Suite.cmd        = main app (no Node needed)
Start Portal Discover.cmd  = extra tool to map the ACC portal (needs Node once)

Node install: open https://nodejs.org → click green LTS → Next, Next, Finish
