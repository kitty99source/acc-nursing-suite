# Pitch mock samples (pointer)

Synthetic boss-pitch import files for **all three suites** (including Admin Suite layouts) live in the Remittance Tracker repo — one pack, no duplicated binaries:

**`/Users/prakritichhetri/ACC-RemittanceTracker/docs/pitch-mock-samples/`**

- Admin Suite mocks: `admin-suite/` (invoice schedule, remittance cross-check, Nursing Toolkit xlsx, NUR02/NUR04VEN letter text+PDF, office-config / email-sync-status)
- README + inventory + integration scoping: `README.md`, `00-inventory.md`, `integration-scope.md`

Zip from Remittance Tracker: `cd docs && zip -r ACC-pitch-mock-samples.zip pitch-mock-samples`
